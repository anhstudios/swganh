include(ANHPlugin)

AddANHPlugin(character
    DEPENDS 
    	libanh
    	libswganh
    ADDITIONAL_INCLUDE_DIRS
        ${Boost_INCLUDE_DIR}
        ${MYSQL_INCLUDE_DIR}
        ${MYSQLCONNECTORCPP_INCLUDE_DIRS}
        ${GLM_INCLUDE_DIR}
        ${GLOG_INCLUDE_DIR}
        ${PYTHON_INCLUDE_DIR}
        ${TBB_INCLUDE_DIRS}
        ${ZLIB_INCLUDE_DIR}
    ADDITIONAL_LIBRARY_DIRS
        ${Boost_LIBRARY_DIRS}
        ${TBB_LIBRARY_DIRS}
    DEBUG_LIBRARIES
        ${MYSQL_LIB}
        ${MYSQLCONNECTORCPP_LIBRARY_DEBUG}
        ${TBB_DEBUG_LIBRARIES}
        ${PYTHON_LIBRARY}
        ${GLOG_LIBRARY_DEBUG}
        ${ZLIB_LIBRARY_DEBUG}
    OPTIMIZED_LIBRARIES
        ${MYSQL_LIB}
        ${MYSQLCONNECTORCPP_LIBRARY_RELEASE}
        ${TBB_LIBRARIES}
        ${PYTHON_LIBRARY}
        ${GLOG_LIBRARY_RELEASE}
        ${ZLIB_LIBRARY_RELEASE}
)

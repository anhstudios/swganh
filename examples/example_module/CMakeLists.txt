include(ANHModule)

AddANHModule(example_module
    DEPENDS 
        anh_lib
        example_app_lib
    ADDITIONAL_INCLUDE_DIRS
        ${Boost_INCLUDE_DIR}
        ${MYSQL_INCLUDE_DIR}
        ${MYSQLCONNECTORCPP_INCLUDE_DIRS}
        ${GLM_INCLUDE_DIR}
        ${PYTHON_INCLUDE_DIR}
		${ZLIB_INCLUDE_DIR}
    ADDITIONAL_LIBRARY_DIRS
        ${Boost_LIBRARY_DIRS}
        ${TBB_LIBRARY_DIRS}
    DEBUG_LIBRARIES 
        ${MYSQL_LIBRARY_DEBUG}
        ${MYSQLCONNECTORCPP_LIBRARY_DEBUG}
        ${PYTHON_LIBRARY}
		${ZLIB_LIBRARY_DEBUG}
    OPTIMIZED_LIBRARIES 
        ${MYSQL_LIBRARY_RELEASE}
        ${MYSQLCONNECTORCPP_LIBRARY_RELEASE}
        ${PYTHON_LIBRARY}
		${ZLIB_LIBRARY_RELEASE}
)
